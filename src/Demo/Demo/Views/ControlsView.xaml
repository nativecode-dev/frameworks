<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:NativeCode.Mobile.Core.XamarinForms.Controls;assembly=NativeCode.Mobile.Core.XamarinForms"
             x:Class="Demo.Views.ControlsView" IsBusy="{Binding IsBusy}" Title="{Binding Title}">
  <StackLayout>
    <controls:SpinSelector ItemsSource="{Binding Items}" SelectedItem="{Binding CurrentItem}" />
  </StackLayout>
</ContentPage>
